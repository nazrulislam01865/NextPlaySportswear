{{--
    Legacy template retained for backwards compatibility.
    CheckoutController::placeOrder redirects to checkout.review, where review
    and final order placement are handled on one page.
--}}
