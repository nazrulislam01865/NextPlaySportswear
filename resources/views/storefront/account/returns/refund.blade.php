<x-storefront.account.orders.page :seo="$seo" :account="$account" :navigation="$navigation" title="Refund Status" :subtitle="'Track '.$refund->refund_number.' amount, destination, provider reference, processing state, bank-posting expectations, and credit-note access.'" eyebrow="Refund tracking">
    <x-slot:actions><a class="btn btn-white" href="{{ route('account.returns.index') }}">Return History</a></x-slot:actions>
    <div class="grid gap-6 lg:grid-cols-[minmax(0,1fr)_330px] lg:items-start"><div class="space-y-6"><section class="rounded-[28px] border border-slate-200 bg-white p-5 shadow-card md:p-7"><div class="flex flex-col justify-between gap-4 sm:flex-row"><div><p class="text-xs font-black uppercase tracking-wide text-brand-red">{{ $refund->refund_number }}</p><h3 class="mt-2 text-2xl font-black">{{ $refund->statusLabel() }}</h3><p class="mt-1 text-sm text-slate-500">Order {{ $refund->order->order_number }} @if($refund->returnRequest)· Return {{ $refund->returnRequest->return_number }}@endif</p></div><x-storefront.account.orders.status-pill :status="$refund->status" /></div>@if($refund->status==='issued')<div class="mt-5 rounded-2xl border border-emerald-200 bg-emerald-50 p-4 text-sm leading-6 text-emerald-800"><b>Refund issued.</b> Most payment providers and banks need several business days to post the credit. Contact your bank with the provider reference only after the expected window.</div>@endif</section><section class="rounded-[28px] border border-slate-200 bg-white p-5 shadow-card md:p-7"><h3 class="text-xl font-black">Refund Timeline</h3><div class="mt-5 grid gap-5">@foreach(['pending'=>'Refund approved','processing'=>'Submitted to payment provider','issued'=>'Provider accepted refund'] as $key=>$label)<div class="flex gap-3"><span class="mt-1 h-3 w-3 rounded-full {{ array_search($refund->status,['pending','processing','issued','failed','cancelled']) >= array_search($key,['pending','processing','issued','failed','cancelled']) ? 'bg-brand-red':'bg-slate-200' }}"></span><div><p class="font-black">{{ $label }}</p><p class="text-sm text-slate-500">{{ $key==='issued' && $refund->processed_at ? $refund->processed_at->format('M d, Y · g:i A') : 'Status updates when processing advances.' }}</p></div></div>@endforeach</div></section></div><aside class="rounded-[28px] border border-slate-200 bg-white p-5 shadow-card"><h3 class="font-black">Refund Summary</h3><div class="mt-4 grid gap-4 text-sm"><div><span class="block text-xs font-black uppercase text-slate-400">Amount</span><strong class="text-2xl">${{ number_format((float)$refund->amount,2) }} {{ $refund->currency }}</strong></div><div><span class="block text-xs font-black uppercase text-slate-400">Destination</span><strong>{{ str($refund->method)->headline() }}</strong></div><div><span class="block text-xs font-black uppercase text-slate-400">Provider reference</span><strong class="break-all">{{ $refund->provider_reference ?: 'Pending' }}</strong></div></div>@if($refund->creditNote)<a class="btn btn-white mt-5 w-full" href="{{ route('account.credit-notes.show',$refund->creditNote) }}">View Credit Note</a>@endif</aside></div>
</x-storefront.account.orders.page>
