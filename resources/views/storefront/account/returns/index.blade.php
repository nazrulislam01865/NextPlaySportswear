<x-storefront.account.orders.page :seo="$seo" :account="$account" :navigation="$navigation" title="Return History" subtitle="Review every return and exchange request, approval, shipment, inspection, refund, credit note, and final resolution." eyebrow="Returns & exchanges">
    <x-slot:actions><a class="btn btn-white" href="{{ route('account.orders.index') }}">Order History</a></x-slot:actions>
    <div class="grid gap-5">@forelse($returns as $return)<article class="rounded-[28px] border border-slate-200 bg-white p-5 shadow-card md:p-7"><div class="flex flex-col justify-between gap-4 sm:flex-row"><div><a href="{{ route('account.returns.show',$return) }}" class="text-xl font-black text-brand-blue hover:text-brand-red">{{ $return->return_number }}</a><p class="mt-1 text-sm text-slate-500">{{ ucfirst($return->type) }} · Order {{ $return->order->order_number }} · Requested {{ $return->requested_at?->format('M d, Y') }}</p></div><x-storefront.account.orders.status-pill :status="$return->status" /></div><div class="mt-5 grid gap-3 md:grid-cols-3"><div class="rounded-2xl bg-slate-50 p-4"><span class="text-xs font-black uppercase text-slate-400">Items</span><p class="mt-1 font-black">{{ $return->items->sum('quantity') }}</p></div><div class="rounded-2xl bg-slate-50 p-4"><span class="text-xs font-black uppercase text-slate-400">Resolution</span><p class="mt-1 font-black">{{ str($return->requested_resolution)->headline() }}</p></div><div class="rounded-2xl bg-slate-50 p-4"><span class="text-xs font-black uppercase text-slate-400">Approved amount</span><p class="mt-1 font-black">{{ $return->approved_amount !== null ? '$'.number_format((float)$return->approved_amount,2) : 'Pending review' }}</p></div></div><div class="mt-5 flex flex-wrap gap-2"><a class="btn btn-white" href="{{ route('account.returns.show',$return) }}">View Details</a>@foreach($return->refunds as $refund)<a class="btn btn-light" href="{{ route('account.refunds.show',$refund) }}">Refund {{ $refund->refund_number }}</a>@endforeach</div></article>@empty<div class="rounded-[28px] border border-slate-200 bg-white p-10 text-center shadow-card"><h3 class="text-xl font-black">No returns or exchanges</h3><p class="mt-2 text-sm text-slate-500">Eligible actions appear on delivered order details pages.</p></div>@endforelse</div><div class="mt-6">{{ $returns->links() }}</div>
</x-storefront.account.orders.page>
