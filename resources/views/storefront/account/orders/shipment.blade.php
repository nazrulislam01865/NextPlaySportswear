<x-storefront.account.orders.page :seo="$seo" :account="$account" :navigation="$navigation" title="Shipment Details" :subtitle="'Carrier, tracking, package contents, address, and delivery progress for '.$shipment->shipment_number.'.'" eyebrow="Package tracking">
    <x-slot:actions><a class="btn btn-white" href="{{ route('account.orders.shipments',$order) }}">All Shipments</a></x-slot:actions>
    <div class="grid gap-6 lg:grid-cols-[minmax(0,1fr)_330px] lg:items-start"><div class="space-y-6">
        <section class="rounded-[28px] border border-slate-200 bg-white p-5 shadow-card md:p-7"><div class="flex flex-col justify-between gap-4 sm:flex-row"><div><p class="text-xs font-black uppercase tracking-wide text-brand-red">{{ $shipment->shipment_number }}</p><h3 class="mt-2 text-2xl font-black">{{ $shipment->statusLabel() }}</h3><p class="mt-2 text-sm text-slate-500">{{ $shipment->carrier ?: 'Carrier pending' }} {{ $shipment->service }} · {{ $shipment->tracking_number ?: 'Tracking number pending' }}</p></div><x-storefront.account.orders.status-pill :status="$shipment->status" /></div>@if($shipment->tracking_url)<a class="btn btn-red mt-5" href="{{ $shipment->tracking_url }}" rel="noopener noreferrer" target="_blank">Open Carrier Tracking ↗</a>@endif</section>
        <section class="rounded-[28px] border border-slate-200 bg-white p-5 shadow-card md:p-7"><h3 class="text-xl font-black">Package Contents</h3><div class="mt-4 grid gap-3">@foreach($shipment->items as $shipmentItem)<div class="grid gap-3 rounded-2xl border border-slate-200 p-4 sm:grid-cols-[1fr_auto]"><div><p class="font-black">{{ $shipmentItem->orderItem->product_name }}</p><p class="text-sm text-slate-500">{{ $shipmentItem->orderItem->sku ?: 'Custom product' }}</p></div><strong>Qty {{ $shipmentItem->quantity }}</strong></div>@endforeach</div></section>
        <section class="rounded-[28px] border border-slate-200 bg-white p-5 shadow-card md:p-7"><h3 class="text-xl font-black">Delivery Progress</h3><div class="mt-5 space-y-4">@foreach(['label_created'=>'Label created','in_transit'=>'In transit','out_for_delivery'=>'Out for delivery','delivered'=>'Delivered'] as $key=>$label)<div class="flex items-center gap-3"><span class="h-3 w-3 rounded-full {{ array_search($shipment->status,array_keys(config('commerce.shipment_statuses',[]))) >= array_search($key,array_keys(config('commerce.shipment_statuses',[]))) ? 'bg-brand-red':'bg-slate-200' }}"></span><span class="font-bold">{{ $label }}</span></div>@endforeach</div></section>
    </div><aside class="rounded-[28px] border border-slate-200 bg-white p-5 shadow-card"><h3 class="font-black">Shipment Summary</h3><div class="mt-4 grid gap-4 text-sm"><div><span class="block text-xs font-black uppercase text-slate-400">Order</span><a class="font-black text-brand-blue" href="{{ route('account.orders.show',$order) }}">{{ $order->order_number }}</a></div><div><span class="block text-xs font-black uppercase text-slate-400">Shipped</span><strong>{{ $shipment->shipped_at?->format('M d, Y · g:i A') ?: 'Pending' }}</strong></div><div><span class="block text-xs font-black uppercase text-slate-400">Estimated delivery</span><strong>{{ $shipment->estimated_delivery_at?->format('M d, Y') ?: 'To be confirmed' }}</strong></div><div><span class="block text-xs font-black uppercase text-slate-400">Delivered</span><strong>{{ $shipment->delivered_at?->format('M d, Y · g:i A') ?: 'Not yet' }}</strong></div></div></aside></div>
</x-storefront.account.orders.page>
