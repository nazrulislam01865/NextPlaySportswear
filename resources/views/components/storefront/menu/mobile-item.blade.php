@props(['item','depth'=>0])
@php($children=$item->childrenRecursive ?? collect())
@if($children->isNotEmpty())<details class="border-b border-slate-100 py-1" {{ $depth===0 ? '' : '' }}><summary class="flex cursor-pointer items-center justify-between rounded-lg px-2 py-2 font-extrabold"><span>{{ $item->label }}</span><span>+</span></summary><div class="space-y-1 border-l border-slate-200 py-1 pl-3">@if($item->resolvedUrl()!=='#')<a class="block rounded-lg px-2 py-2 text-brand-red" href="{{ $item->resolvedUrl() }}" target="{{ $item->target }}" @if($item->target==='_blank') rel="noopener noreferrer" @endif>View all {{ $item->label }}</a>@endif @foreach($children as $child)<x-storefront.menu.mobile-item :item="$child" :depth="$depth+1" />@endforeach</div></details>@else<a class="block rounded-lg px-2 py-2 hover:bg-slate-100" href="{{ $item->resolvedUrl() }}" target="{{ $item->target }}" @if($item->target==='_blank') rel="noopener noreferrer" @endif>{{ $item->label }}</a>@endif
