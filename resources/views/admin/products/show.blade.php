<x-layouts.admin :title="$product->name">
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:flex-wrap sm:justify-end"><a href="{{ route('products.show', $product->slug) }}" target="_blank" class="btn btn-white">View Storefront</a><a href="{{ route('admin.products.edit', $product) }}" class="btn btn-red">Edit Product</a></div>
    <div class="grid gap-6 lg:grid-cols-[360px_1fr]">
        <section class="rounded-3xl border border-slate-200 bg-white p-5 shadow-card"><img src="{{ $product->primaryImageUrl() }}" alt="{{ $product->name }}" class="aspect-square w-full rounded-2xl object-cover"><div class="mt-4 grid gap-2 text-sm"><p><strong>SKU:</strong> {{ $product->sku }}</p><p><strong>Status:</strong> {{ ucfirst($product->status) }}</p><p><strong>Featured:</strong> {{ $product->is_featured ? 'Yes' : 'No' }}</p><p><strong>Customizable:</strong> {{ $product->is_customizable ? 'Yes' : 'No' }}</p></div></section>
        <section class="rounded-3xl border border-slate-200 bg-white p-6 shadow-card"><h2 class="text-3xl font-black">{{ $product->name }}</h2><p class="mt-3 text-slate-600">{{ $product->short_description }}</p><div class="mt-6 grid gap-4 sm:grid-cols-3"><div><p class="text-xs uppercase text-slate-400">Base price</p><p class="text-xl font-black">{{ $product->currency }} {{ number_format((float)$product->base_price,2) }}</p></div><div><p class="text-xs uppercase text-slate-400">Option groups</p><p class="text-xl font-black">{{ $product->optionGroups->count() }}</p></div><div><p class="text-xs uppercase text-slate-400">Price tiers</p><p class="text-xl font-black">{{ $product->priceTiers->count() }}</p></div></div><div class="prose mt-8 max-w-none">{!! $product->description_html !!}</div></section>
    </div>
</x-layouts.admin>
