<x-layouts.admin title="Navigation Menus"><div class="mb-6 flex items-center justify-between gap-3"><p class="text-sm text-slate-500">Control header mega menus, mobile navigation, footer columns, and category links without editing Blade templates.</p><a class="btn btn-red" href="{{ route('admin.menus.create') }}">+ Add Menu</a></div><div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3">@forelse($menus as $menu)<article class="rounded-3xl border border-slate-200 bg-white p-5 shadow-card"><div class="flex items-start justify-between gap-3"><div><h2 class="text-lg font-black text-brand-ink">{{ $menu->name }}</h2><p class="mt-1 text-xs text-slate-500">{{ $menu->location ?: 'Unassigned location' }} · {{ $menu->all_items_count }} items</p></div><span class="rounded-full px-2 py-1 text-xs font-bold {{ $menu->is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600' }}">{{ $menu->is_active ? 'Active' : 'Inactive' }}</span></div><div class="mt-5 flex gap-2"><a class="btn btn-white flex-1" href="{{ route('admin.menus.edit',$menu) }}">Edit</a><form method="POST" action="{{ route('admin.menus.destroy',$menu) }}" onsubmit="return confirm('Delete this menu?');">@csrf @method('DELETE')<button class="btn btn-white text-red-700">Delete</button></form></div></article>@empty<div class="rounded-3xl border border-dashed border-slate-300 bg-white p-10 text-center text-slate-500">No navigation menus exist.</div>@endforelse</div></x-layouts.admin>
