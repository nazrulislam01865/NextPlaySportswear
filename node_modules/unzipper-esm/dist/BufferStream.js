"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = readAsBuffer;
var _stream = require("stream");
// 'node:stream'

/**
 * Reads an entry as a `Buffer`.
 * @param {*} entry
 * @returns {Promise<Buffer>}
 */
function readAsBuffer(entry) {
  return new Promise(function (resolve, reject) {
    var chunks = [];
    var bufferStream = new _stream.Transform({
      transform: transform
    }).on('finish', function () {
      resolve(Buffer.concat(chunks));
    }).on('error', reject);
    function transform(d, e, cb) {
      chunks.push(d);
      cb();
    }
    ;
    entry.on('error', reject).pipe(bufferStream);
  });
}
;
//# sourceMappingURL=BufferStream.js.map